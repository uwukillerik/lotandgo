# APK — Android SDK (без Expo)

```powershell
$env:ANDROID_HOME = "$env:LOCALAPPDATA\Android\Sdk"
pnpm build:apk
```

Подписанный APK: `public/downloads/lotgo.apk`

Проект: `android-app/` — открывается в Android Studio.
